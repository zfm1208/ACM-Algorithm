#include<bits/stdc++.h>
using namespace std;
#define rep(i, l, r) for (int i = l; i <= r; i++)
#define vii vector<int>
#define pii pair<int, int>
#define int long long
#define pb push_back
#define fi first
#define se second
#define endl '\n'
double pi = acos(-1);
const int N = 1e6, mod = 1e9+7, inf = 1e18 + 5;

void solve(){
    
}

signed main(){
    ios::sync_with_stdio(false);
    cin.tie(0);cout.tie(0);
    int T = 1;
    cin >> T;
    while(T--)
        solve();
    return 0;
}
/*
    Ctrl + Alt + B：CPH 运行样例，也就是你右侧那个测试面板的“运行全部”
    Ctrl + Alt + J：聚焦 CPH 测评器面板
    Ctrl + Alt + S：CPH 提交 Codeforces
    Ctrl + Alt + Shift + B：Debug 编译当前 C++ 文件
    Ctrl + Alt + R：Release 编译并运行当前 C++ 文件
    Ctrl + Alt + G：启动 GDB 调试当前文件
    Ctrl + Alt + P：对拍当前文件

*/