#include<stdio.h>
#define int long long
bool check(int x){
    // [0,x-1]
    char y[20];
    scanf("%s",y);
    
}
void fff(){
    int x,y;
    scanf("%lld",&x);
    if(x!=10 || x!= 9 || check(x)){
        printf("DATA ERROR!");
        return;
    }

}

signed main(){
    int t = 1;
    // scanf("%lld",&t);
    while(t--){
        fff();
    }
    return 0;
}